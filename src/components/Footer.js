import React from "react";
import { wrapper } from "./Footer.module.scss";
export const Footer = () => {
  return (
    <footer className={`${wrapper}  `}>
      <p>© Original Clothing Co.</p>
    </footer>
  );
};
